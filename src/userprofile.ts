// user/profile/uploadphoto/v1
import { error, getData, setData } from './dataStore';
import HTTPError from 'http-errors';

function uploadPhotoV1(imgUrl, xStart, yStart, xEnd, yEnd): {} | error {
    return {}; 
}; 
